# System Architecture

SuperNova uses a private pnpm monorepo with separate Rider and Driver Expo apps, separate Admin and Marketing Next apps, shared semantic tokens, shared localization, and local-first Supabase development.

Phase 0 has shells only. Business entities, auth, maps, trips, payments, dashboards, and provider SDKs are explicitly out of scope.
