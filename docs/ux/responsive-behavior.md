# Responsive Behavior

Admin and Marketing use fluid grids from mobile width through desktop. Tables remain horizontally usable on small screens. Marketing hero type uses clamped CSS sizes and no viewport-only font scaling in compact components.

Manual review must cover mobile, tablet, laptop, and desktop widths.
