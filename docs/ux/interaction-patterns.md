# Interaction Patterns

Patterns include one primary action per state, expandable bottom sheets, segmented prototype controls, large mobile touch targets, visible web focus states, status pills with text, and visual-only admin actions.

Reduced motion must be respected in production.
