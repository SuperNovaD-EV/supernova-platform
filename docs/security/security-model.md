# Security Model

Phase 0 defines environment boundaries, local-only Supabase, strict TypeScript, no secrets, and no remote database push by default. Future features must document data access, auditing, retention, and abuse controls before implementation.
