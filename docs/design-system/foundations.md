# Foundations

Foundations include semantic tokens, typography roles, spacing, sizing, radii, shadows, motion, focus states, status colors, chart tokens, map UI tokens, and accessibility targets.

Applications should consume semantic tokens rather than raw hex values.
