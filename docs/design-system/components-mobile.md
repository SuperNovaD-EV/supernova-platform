# Mobile Components

`packages/ui-mobile` includes screen, surface, stack, inline, text, buttons, segmented controls, fields, map mock, vehicle card, fare summary, timeline, safety, finance, and support patterns.

Components support theme and locale context.
