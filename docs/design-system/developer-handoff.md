# Developer Handoff

Use token imports from `@supernova/design-tokens`, mobile primitives from `@supernova/ui-mobile`, web primitives from `@supernova/ui-web`, and mock data from `@supernova/testing`.

Do not introduce backend calls, maps SDKs, payment SDKs, auth SDKs, or production credentials in prototype components.
