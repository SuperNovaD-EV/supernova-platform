# Design System Overview

SuperNova's Phase 2 design system centralizes tokens in `packages/design-tokens`, mobile components in `packages/ui-mobile`, web components in `packages/ui-web`, and web charts in `packages/charts-web`.

It is prototype-ready, not a final production UI implementation.
