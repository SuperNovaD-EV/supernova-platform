# Map UI

Map UI is a mock system with fictional route shapes, pickup, destination, driver marker, accuracy, and zone representation. It must never use Google Maps calls or copied map imagery in Phase 2.
