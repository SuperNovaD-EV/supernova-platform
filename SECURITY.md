# Security

Report security issues privately to the repository owner once ownership is configured. Do not file public issues containing secrets, vulnerabilities, or personal data.

Public variables start with `EXPO_PUBLIC_` or `NEXT_PUBLIC_`. Server-only values such as `SUPABASE_SECRET_KEY`, `SUPABASE_DB_PASSWORD`, `SENTRY_AUTH_TOKEN`, and project ownership settings must never be exposed to clients.
