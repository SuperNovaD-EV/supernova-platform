export const smokeLocales = ["en", "ar"] as const;

export * from "./fixtures/prototype-data";

export const testIds = {
  shell: "supernova-shell",
  criticalAction: "supernova-critical-action",
} as const;
